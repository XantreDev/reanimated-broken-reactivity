diff --git a/node_modules/react-native-reanimated/lib/module/reanimated2/hook/useAnimatedReaction.js b/node_modules/react-native-reanimated/lib/module/reanimated2/hook/useAnimatedReaction.js
index 1570f50..0c432f6 100644
--- a/node_modules/react-native-reanimated/lib/module/reanimated2/hook/useAnimatedReaction.js
+++ b/node_modules/react-native-reanimated/lib/module/reanimated2/hook/useAnimatedReaction.js
@@ -1,4 +1,5 @@
 import { useEffect, useRef } from 'react';
+import { useSharedValue } from 'react-native-reanimated';
 import { startMapper, stopMapper } from '../core';
 import { shouldBeUseWeb } from '../PlatformChecker';
 /**
@@ -8,9 +9,7 @@ import { shouldBeUseWeb } from '../PlatformChecker';
  * the second one can modify any shared values but those which are mentioned in the first worklet. Beware of that, because this may result in endless loop and high cpu usage.
  */
 export function useAnimatedReaction(prepare, react, dependencies) {
-  const previous = useRef({
-    value: null
-  }).current;
+  const previous = useSharedValue(null, true)
   let inputs = Object.values(prepare._closure ?? {});
   if (shouldBeUseWeb()) {
     var _dependencies;
