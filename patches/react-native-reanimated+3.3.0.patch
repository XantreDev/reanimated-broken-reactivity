diff --git a/node_modules/react-native-reanimated/lib/module/reanimated2/hook/useAnimatedReaction.js b/node_modules/react-native-reanimated/lib/module/reanimated2/hook/useAnimatedReaction.js
index 1570f50..dee14a5 100644
--- a/node_modules/react-native-reanimated/lib/module/reanimated2/hook/useAnimatedReaction.js
+++ b/node_modules/react-native-reanimated/lib/module/reanimated2/hook/useAnimatedReaction.js
@@ -1,4 +1,5 @@
 import { useEffect, useRef } from 'react';
+import { useSharedValue } from './useSharedValue';
 import { startMapper, stopMapper } from '../core';
 import { shouldBeUseWeb } from '../PlatformChecker';
 /**
@@ -8,9 +9,7 @@ import { shouldBeUseWeb } from '../PlatformChecker';
  * the second one can modify any shared values but those which are mentioned in the first worklet. Beware of that, because this may result in endless loop and high cpu usage.
  */
 export function useAnimatedReaction(prepare, react, dependencies) {
-  const previous = useRef({
-    value: null
-  }).current;
+  const previous = useSharedValue(null, true)
   let inputs = Object.values(prepare._closure ?? {});
   if (shouldBeUseWeb()) {
     var _dependencies;
diff --git a/node_modules/react-native-reanimated/src/reanimated2/hook/useAnimatedReaction.ts b/node_modules/react-native-reanimated/src/reanimated2/hook/useAnimatedReaction.ts
index 10441c3..f7ed1bf 100644
--- a/node_modules/react-native-reanimated/src/reanimated2/hook/useAnimatedReaction.ts
+++ b/node_modules/react-native-reanimated/src/reanimated2/hook/useAnimatedReaction.ts
@@ -1,8 +1,9 @@
-import { useEffect, useRef } from 'react';
+import { useEffect } from 'react';
 import { BasicWorkletFunction, WorkletFunction } from '../commonTypes';
 import { startMapper, stopMapper } from '../core';
 import { DependencyList } from './commonTypes';
 import { shouldBeUseWeb } from '../PlatformChecker';
+import { useSharedValue } from './useSharedValue';
 
 export interface AnimatedReactionWorkletFunction<T> extends WorkletFunction {
   (prepared: T, previous: T | null): void;
@@ -18,7 +19,7 @@ export function useAnimatedReaction<T>(
   react: AnimatedReactionWorkletFunction<T>,
   dependencies: DependencyList
 ): void {
-  const previous = useRef({ value: null as T | null }).current;
+  const previous = useSharedValue<T | null>(null, true)
 
   let inputs = Object.values(prepare._closure ?? {});
 
